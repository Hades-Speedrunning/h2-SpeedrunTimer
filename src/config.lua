return {
  version = "0.0.2",
  enabled = true,
  ModName = "SpeedrunTimer",
  trueUpInterval = 30, -- how often to call trueUp in frames. More frequent means more accurate but also more expensive.
}
